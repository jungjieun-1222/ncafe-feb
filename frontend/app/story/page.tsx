import StoryContent from './StoryContent';

export const metadata = {
  title: '이야기',
  description: '엔카페(ncafe)의 시작과 계절이 머무는 풍경 이야기',
};

export default function StoryPage() {
  return <StoryContent />;
}
